@model List<Post>

<div class="row">
    <div class="col-12">
        <h2>Post Listesi</h2>
        <a href="/posts/create" class="btn btn-primary">Yeni Post</a>
        <table class="table tabled-bordered mt-3">
            <thead>
                <tr>
                    <th style="width: 200px;">Görüntü</th>
                    <th>Title</th>
                    <th>Is Active</th>
                    <th style="width: 125px;"></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <td><img src="/img/@item.Image" class="img-fluid" alt=""></td>
                        <td>@item.Title</td>
                        <td>@item.IsActive</td>
                        <td>
                            <a href="/posts/edit/@item.PostID" class="btn btn-sm btn-primary">Güncelle</a>
                            <a href="#" class="btn btn-sm btn-danger">Sil</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

